import { MySPlantasValidators } from './my-splantas-validators';

describe('MySPlantasValidators', () => {
  it('should create an instance', () => {
    expect(new MySPlantasValidators()).toBeTruthy();
  });
});
